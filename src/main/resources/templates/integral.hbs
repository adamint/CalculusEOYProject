{{> header.hbs }}

<div>
    <div class="uk-margin-small-top uk-section uk-section-large">
        <div class="uk-text-center">
            <h3 class="light">Integral Rule | {{integral.readable}}</h3>
            <p>See <a class="rem-1" href="/integrals/all">all rules →</a></p>

            <div id="integral">
                $$ \int [{{integral.before}}] du= {{integral.after}} + C$$

                {{#if integral.note}}
                    <p><i>Note: </i> {{integral.note}}</p>
                {{/if}}
            </div>
        </div>
    </div>
</div>

{{> footer.hbs }}