{{> header.hbs }}

<div>
    <div class="uk-margin-small-top uk-section uk-section-large">
        <h2 class="light uk-text-center">Multiple Notes Found > Section {{chapter.chapterNumber}}.{{section.sectionNumber}}</h2>

        <div class="uk-text-center uk-margin-auto uk-margin-remove-bottom js-upload uk-placeholder uk-width-1-4">
            {{#each notes-options}}
                <h3 class="uk-margin-small-top moderate-bold">
                    <a href="/resources/{{& first}}">{{second}} {{> open-in-new.hbs}}</a>
                </h3>
            {{/each}}
        </div>
    </div>
</div>

{{> footer.hbs }}